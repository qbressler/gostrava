package utils

import "testing"

func TestCodeURL(t *testing.T) {
	/*
		strURL := "https://www.strava.com/oauth/authorize?client_id=18876&response_type=code&redirect_uri=http://localhost/exchange_token&approval_prompt=force&scope=read,activity:read"
		result := GetCodeFromURL(strURL, "code")
		if result == "" {
			t.Fail()
		}
		t.Log(result)
	*/

}
